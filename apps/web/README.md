# apps/web

Next.js frontend for SMMOMO.

The root `README.md` at the repository top level is the source of truth for
setup, commands, and architecture. This file exists only to satisfy the
workspace layout.
